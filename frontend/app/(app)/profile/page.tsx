import { Stub } from "@/components/layout/stub";

export default function ProfilePage() {
  return (
    <Stub
      title="Profile"
      owner="partner"
      blurb="Name, education, skills, projects, certifications, CV upload and target role — served by profile-api."
    />
  );
}
