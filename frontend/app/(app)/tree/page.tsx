import { Stub } from "@/components/layout/stub";

export default function TreePage() {
  return (
    <Stub
      title="Skill Tree"
      owner="Azeem"
      blurb="d3 circle packing over the same skills read as a tree: you at the root, categories, subcategories, then leaves sized by mastery."
    />
  );
}
