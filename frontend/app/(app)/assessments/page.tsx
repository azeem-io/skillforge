import { Stub } from "@/components/layout/stub";

export default function AssessmentsPage() {
  return (
    <Stub
      title="Assessments"
      owner="partner"
      blurb="Recall, cloze and MCQ questions per skill, scored into a per-skill breakdown that feeds studentSkills and the FSRS schedule."
    />
  );
}
